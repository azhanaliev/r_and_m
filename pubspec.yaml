name: r_and_m
description: clean arch project

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.18.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  json_annotation: ^4.7.0
  freezed_annotation: ^2.1.0
  http: ^0.13.5
  flutter_bloc: ^8.1.1
  hydrated_bloc: ^8.1.0
  path_provider: ^2.0.11
  cached_network_image: ^3.2.2
  pull_to_refresh: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  build_runner: ^2.2.1
  freezed: ^2.1.1
  json_serializable: ^6.4.0

flutter:

  uses-material-design: true
